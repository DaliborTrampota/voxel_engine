- geometry shaders
- materials
- lights
- shadows
- ui
    - basic ui elements @done
    - input elements
    - fallback for bindless textures
- get rid of engine namespace in lwgl events
- vulcan?
- texture keep track of bound units and warn if two textures are bound to the same unit at the same time
- get rid of gl::texture namespace @done
- move textureprivate to detail @done
- glfw callbacks using window user pointer
- fix touching blocks when stepping up
